import { TConfig, TProfile, TLastPosts, TPost } from '../entities/types.js';
declare class InstagramApiService {
    private request;
    private proxy;
    constructor(config: TConfig);
    getProfile(username: string): Promise<TProfile>;
    getLastPosts(username: string): Promise<TLastPosts>;
    getPost(postUrl: string): Promise<TPost>;
}
export { InstagramApiService as Instagrapi };
